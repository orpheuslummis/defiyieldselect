version: "3.8"
services:
  forecasting:
    build: .
    command: python forecasting/run.py
    volumes:
      - results_vol:/results/
      - data_vol:/data/
    env_file: env.list
  # modelsearch:
  #   build: .
  #   command: python modelsearch/run.py
  #   volumes:
  #     - results_vol:/results/
  #     - data_vol:/data/
  #   env_file: env.list
  # collect_orcadefi:
  #   build: .
  #   command: python datacollect/collect_orcadefi.py
  #   volumes: [data_vol:/data/]
volumes:
  data_vol:
  results_vol: